{
  "name": "@archwayhq/keyring-go",
  "version": "0.1.0",
  "description": "A Node.js native addon for the Cosmos Keyring Go library to store values using the OS keyring or the filesystem",
  "author": "Archway Network (https://archway.io)",
  "license": "Apache-2.0",
  "main": "src/node/index.js",
  "gypfile": true,
  "dependencies": {
    "node-addon-api": "^7.0.0",
    "node-gyp-build": "^4.6.0",
    "node-gyp": "^9.4.0"
  },
  "devDependencies": {
    "prebuildify": "^5.0.1",
    "rimraf": "^5.0.1"
  },
  "scripts": {
    "clean": "rimraf ./build",
    "build": "npm run build:cgo && npm run build:pre",
    "build:cgo": "./build.sh",
    "build:pre": "prebuildify --napi --strip",
    "install": "node-gyp-build",
    "test": "node --napi-modules ./test/*.js"
  }
}
